import type { InsiderTransaction, ApiProvider } from "../types";

// TODO: Implement insider transaction normalization
export function normalizeInsiderTransactions(raw: unknown, source: ApiProvider): InsiderTransaction[] {
  return [];
}
