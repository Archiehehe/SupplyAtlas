import type { Company, ApiProvider } from "../types";

// TODO: Implement company profile normalization
export function normalizeCompanyProfile(raw: unknown, source: ApiProvider): Company | null {
  return null;
}
