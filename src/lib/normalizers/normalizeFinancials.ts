import type { CompanyMetric, ApiProvider } from "../types";

// TODO: Implement financials normalization
export function normalizeFinancials(raw: unknown, source: ApiProvider): CompanyMetric[] {
  return [];
}
