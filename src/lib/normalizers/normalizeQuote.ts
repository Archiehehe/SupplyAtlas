import type { CompanyMetric, ApiProvider } from "../types";

// TODO: Implement quote normalization
export function normalizeQuote(raw: unknown, source: ApiProvider): CompanyMetric[] {
  return [];
}
