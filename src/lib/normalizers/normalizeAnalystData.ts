import type { CompanyMetric, ApiProvider } from "../types";

// TODO: Implement analyst data normalization
export function normalizeAnalystData(raw: unknown, source: ApiProvider): CompanyMetric[] {
  return [];
}
