import type { Transcript, ApiProvider } from "../types";

// TODO: Implement transcript normalization
export function normalizeTranscript(raw: unknown, source: ApiProvider): Transcript | null {
  return null;
}
