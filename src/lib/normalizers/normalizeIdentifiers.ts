import type { CompanyIdentifier, ApiProvider } from "../types";

// TODO: Implement identifier normalization
export function normalizeIdentifiers(raw: unknown, source: ApiProvider): CompanyIdentifier[] {
  return [];
}
