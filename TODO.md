# TODO
- [x] Verify whether `vercel.json` exists in repo.
- [x] Remove `crons` by deleting `vercel.json` (if present).
- [ ] Search repo for any other Vercel cron/scheduler config or cron-related API endpoints.
- [ ] Remove any cron-only API routes and associated docs/comments (only if clearly cron-only).
- [ ] Run checks: install (if needed), lint, typecheck, build.
- [ ] Provide final report of changed files and rationale.
